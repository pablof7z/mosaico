---
type: noun-entry
slug: routing-scope
name: "routing scope"
origin: extracted
source_refs:
  - transcript:258-274
  - transcript:158-179
---

# routing scope

The NIP-29 group id this session currently routes under — its channel when set (a `channels switch` moved it to a subgroup), else its per-session room (`project`)
